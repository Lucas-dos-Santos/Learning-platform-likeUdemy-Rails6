= link_to 'Courses', courses_path, class: 'text-primary'
\/
= link_to @course.title, course_path(@course), class: 'text-primary'

.row
	.col-lg-6
		= render @course
	.col-lg-6
		= render @course.lessons
